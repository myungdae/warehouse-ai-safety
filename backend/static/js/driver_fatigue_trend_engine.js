(function(g){'use strict';const C=g.DriverRuntimeMemoryConfig,finite=v=>v==null||v===''?null:Number.isFinite(Number(v))?Number(v):null;class DriverFatigueTrendEngine{evaluate({current={},previous={},windowCode='FIVE_MINUTES',comparedWindowCode='PREVIOUS_FIVE_MINUTES',confidence=1}={}){const keys=['PERCLOS','YAWN','LONG_EYE_CLOSURE','ATTENTION_LOSS','HEAD_DOWN','DROWSINESS_RISK','VOICE_ESCALATION'],signals=[],opposing=[];let score=0,evidence=0;for(const k of keys){const a=finite(current[k]),b=finite(previous[k]);if(a==null||b==null)continue;evidence++;const d=a-b,w=k==='PERCLOS'?30:k==='LONG_EYE_CLOSURE'?22:12;if(d>0){score+=Math.min(w,d*w);signals.push({signal:k,delta:d})}else if(d<0){score+=Math.max(-w,d*w);opposing.push({signal:k,delta:d})}}const conf=Math.max(0,Math.min(1,finite(confidence)??0));let trend='INSUFFICIENT_DATA';const reasons=[];if(!evidence){trend='UNKNOWN';reasons.push('NO_COMPARABLE_EVIDENCE')}else if(evidence<C.minimumTrendEvidence){reasons.push('MINIMUM_EVIDENCE_NOT_MET')}else{const adjusted=score*conf;if(adjusted<=-20)trend='IMPROVING';else if(adjusted<10)trend='STABLE';else if(adjusted<25)trend='MILD_INCREASE';else if(adjusted<50)trend='INCREASING';else if(adjusted<80)trend='RAPID_INCREASE';else trend='CRITICAL';if(conf<.5&&['RAPID_INCREASE','CRITICAL'].includes(trend))trend='INCREASING';reasons.push(`SCORE_${Math.round(adjusted)}`)}return Object.freeze({trendCode:trend,score:Number.isFinite(score)?score:null,confidence:conf,evidenceCount:evidence,requiredEvidenceCount:C.minimumTrendEvidence,contributingSignals:signals,opposingSignals:opposing,reasonCodes:reasons,windowCode,comparedWindowCode,operationalUseAllowed:false})}}
g.DriverFatigueTrendEngine=Object.freeze({DriverFatigueTrendEngine})})(window);
